#name: CI/CD Deploy to EC2
#
#on:
#  push:
#    branches: [ "main" ]
#
#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Set up JDK 21
#        uses: actions/setup-java@v4
#        with:
#          distribution: temurin
#          java-version: 21
#
#      - name: Build with Maven
#        run: mvn clean package -DskipTests
#
#      - name: Login to Docker Hub
#        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
#
#      - name: Build Docker image
#        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/zkar:latest .
#
#      - name: Push Docker image
#        run: docker push ${{ secrets.DOCKER_USERNAME }}/zkar:latest
#
#      - name: Deploy to EC2 via SSH
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ${{ secrets.EC2_USER }}
#          key: ${{ secrets.EC2_KEY }}
#          script: |
#            docker pull ${{ secrets.DOCKER_USERNAME }}/zkar:latest
#            docker stop video_backend || true
#            docker rm video_backend || true
#            docker run -d \
#              --name video_backend \
#              --network video-net \
#              -p 80:8080 \
#              --env-file .env \
#              ${{ secrets.DOCKER_USERNAME }}/zkar:latest